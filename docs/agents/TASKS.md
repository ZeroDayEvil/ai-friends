# Реестр задач

Статусы: `todo`, `wip`, `review`, `done`, `blocked`.
Обновляется оркестратором при каждом изменении состояния.

| ID | Задача | Исполнитель | Статус | Комментарий |
|---|---|---|---|---|
| control-plane | Репозиторий и контур управления | Агент 1 | done | локально; на GitHub не выложен, нет доступа |
| agent-briefs | Брифы для Агентов 2 и 3 | Агент 1 | done | `docs/agents/briefs/` |
| supply-audit | Реестр внешних загрузок и пинов | Агент 2 | done | 23 записи с пинами, 14 находок; принято, три находки дали правки в форке |
| clean-build | Чистая сборка форка | Агент 1 | done | образ собран и проверен, см. `docs/design/fork.md` |
| multiuser | Многопользовательскость | Агент 1 | wip | личность, владение сессиями и права на машины сделаны; PostgreSQL, изоляция процессов и отзыв активных сессий остались |
| protocols | Протокольные интеграции | Агент 1 | done | в upstream реализовано почти всё, см. `docs/design/protocols.md`; отдельные websockify и goproxy не нужны |
| vps-infra | Развёртывание Linux-узла | Агент 1 | blocked | скрипт написан и проверен на синтаксис, VPS не выдан |
| cf-access | Туннель и политика доступа | Агент 1 | blocked | конфиг и проверка JWT готовы и покрыты тестами, домен не подключён |
| mesh | WireGuard-меш | Агент 1 | blocked | скрипты готовы, нужен узел-концентратор |
| compat-matrix | Матрица совместимости Windows | Агент 3 | wip | Server 2025 принят: дефект устранён, подъём после перезагрузки проверен, приёмка даёт FAIL=0; для пяти остальных ОС нет машин |

## Что готово к применению

Всё, что можно было сделать без внешних ресурсов, сделано и проверено:

- образ платформы собирается из закреплённых версий, запускается, внешние
  ИИ-эндпоинты в нём отключены, токен живёт 12 часов вместо 120 лет;
- режим Cloudflare Access реализован и проверен: без действительного токена
  приложение отвечает 401 на любой запрос, включая WebSocket;
- подключение к чужой сессии по её идентификатору закрыто;
- подключиться к чужой сессии по её идентификатору нельзя, а право сотрудника на
  конкретную машину проверяется на сервере, а не берётся из запроса клиента;
- скрипты развёртывания узла и меша проверены на синтаксис, compose-файл
  проверен, порт приложения привязан только к loopback;
- на тестовом сервере WinRM отключён вместе со службой и слушателями, включена
  NLA, отключён SMBv1, настроены аудит входов и блокировка учётной записи;
- доступ по SSH не зависит от единственного правила брандмауэра, а подъём узла
  после перезагрузки проверен на практике.

Тесты форка: 38 проверок в четырёх файлах, запуск `npm run test:platform`.

Порядок развёртывания, когда появятся ресурсы: `docs/design/runbook.md`.

## Что нужно от владельца инфраструктуры

Эти пункты нельзя закрыть агентами:

1. Приватный репозиторий на GitHub и доступ к нему (или `gh` с авторизацией).
   Сейчас история лежит локально в `C:\electerm-platform`; после появления
   удалённого репозитория она выкладывается одной командой, и станут доступны
   облачные агенты и защита `main`.
2. Linux VPS (Ubuntu 24.04, 2 vCPU, 4 ГБ RAM, 60-80 ГБ SSD), root по ключу.
3. Домен в зоне Cloudflare и аккаунт Zero Trust.
4. Для матрицы совместимости — машины Win10, Win11, Server 2016/2019/2022.
5. Реестр контейнеров для образа платформы (GitHub Packages подойдёт).
